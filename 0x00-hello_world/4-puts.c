#include <stdio.h>
/**
 * main- Serves as the entry point for thr peogram
 *
 * Return: By default returns \ero if no errors otherwise returns non-zero value.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0)
}
